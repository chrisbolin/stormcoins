// PHYSICS CONSTANTS
export const FORCE_GRAVITY = 4.5 / 100000; // note: dt is in milliseconds, so this constant needs to be very small
export const FORCE_ENGINE = 2.5 * FORCE_GRAVITY;
export const COEFFICIENT_DRAG = 1 / 100;

// LAYOUT
export const PLATFORM_LEFT = 30;
export const PLATFORM_WIDTH = 20;
